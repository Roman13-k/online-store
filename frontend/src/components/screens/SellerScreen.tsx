import React from "react";

export default function SellerScreen() {
  return (
    <section>
      {/* {data.map((d: any) => (
        <div key={d.name}>
          <h2>{d.name}</h2>
          <p>{d.count}</p>
          <p>{d.description}</p>
        </div>
      ))}
      {isSuccess && <p>успех</p>} */}
    </section>
  );
}
